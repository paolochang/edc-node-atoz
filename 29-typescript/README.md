# TypeScript

## Requirement

1. Install `typescript`

   ```
   $ npm i -g typescript
   ```

## Setup TypeScript

1. Create `package.json`

   ```
   $ npm init --yes
   ```

2. Create `tsconfig.json`

   ```
   $ tsc --init
   ```
